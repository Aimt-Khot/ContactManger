@model IEnumerable<ContactManger.Models.Contact>

@{
    ViewBag.Title = "Contact App";
}

<h2>Contacts</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using(Html.BeginForm("Index" , "Contact" , FormMethod.Post))
 {
    <div>
    Search By FirstName/LastName @Html.TextBox("searchString")
    <input id="Submit1" type="submit" value="search" />
    </div>
 }
<br />
<p>
    @Html.ActionLink("SortContacts", "Sort")
</p>
<p>
    @Html.ActionLink("ExportToExcel", "Export")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhoneNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmailID)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }
</table>













@*@model IEnumerable<ContactManger.Models.Contact>

@{
    ViewBag.Title = "Search";
}
<b>Search Option:</b>
@using (Html.BeginForm())
{
    <div class="form-horizontal">
        <h4>Contact</h4>
        <div class="col-md-10">
            @Html.RadioButton("option", "FirstName") <text>FirstName</text>
        </div>
        <div class="col-md-10">
            @Html.RadioButton("option", "LastName") <text>LastName</text>
        </div>
        <div class="col-md-10">
            @Html.RadioButton("option", "PhoneNo")<text>PhoneNo</text>
        </div>
        <div class="col-md-10">
            @Html.TextBox("search") < input type = "submit"
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Search" class="btn btn-default" />
            </div>
        </div>
    </div>
}
<div>
    @Html.ActionLink("Back to List", "Index")
</div>*@








@*@model IEnumerable<SearchSortPaging.Models.Student>

    @ {
    ViewBag.Title = "Index";
    }

    @ * Html.BeginForm is used to add a form in an Html document * @@ * Pass the action name in the first parameter, controller name in the second parameter and specify the form request type as get * @

    @using (Html.BeginForm("Index", "Home", FormMethod.Get))
    {*@
@*//the following are search options*@
@*@model IEnumerable<ContactManger.Models.Contact>

    @{
        ViewBag.Title = "Contact Export";
    }

    <!DOCTYPE html>

    <html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>Index</title>
    </head>
    <body>
        <h4>Contacts</h4>
        <hr />
        <table class "table" cellpadding="0" cellspacing="0">
            <tr>
                <th>ContactID</th>
                <th>FirstName</th>
                <th>LastName</th>
                <th>PhoneNo</th>
                <th>EmailID</th>
            </tr>
            @foreach (var contact in Model)
            {
                <tr>
                    <td>@contact.Id</td>
                    <td>@contact.FirstName</td>
                    <td>@contact.LastName</td>
                    <td>@contact.PhoneNo</td>
                    <td>@contact.EmailID</td>
                </tr>
            }
        </table>
        <br />
        <br />
        @using (Html.BeginForm("Export", "Home", FormMethod.Post))
        {
            <input type="submit" value="Export" />
        }
    </body>
    </html>*@